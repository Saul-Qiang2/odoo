<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="mytheme.list">
      <field name="name">mytheme list</field>
      <field name="model">mytheme.mytheme</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="mytheme.action_window">
      <field name="name">mytheme window</field>
      <field name="res_model">mytheme.mytheme</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="mytheme.action_server">
      <field name="name">mytheme server</field>
      <field name="model_id" ref="model_mytheme_mytheme"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="mytheme" id="mytheme.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="mytheme.menu_1" parent="mytheme.menu_root"/>
    <menuitem name="Menu 2" id="mytheme.menu_2" parent="mytheme.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="mytheme.menu_1_list" parent="mytheme.menu_1"
              action="mytheme.action_window"/>
    <menuitem name="Server to list" id="mytheme" parent="mytheme.menu_2"
              action="mytheme.action_server"/>
    -->
  </data>
</odoo>